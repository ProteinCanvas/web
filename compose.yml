services:
  app:
    build:
      context: .
      args:
        BASE_PATH: ${BASE_PATH:-}
    ports:
      - "${PORT:-3000}:80"
    environment:
      BASE_PATH: ${BASE_PATH:-}
    restart: unless-stopped
